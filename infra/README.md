# Infrastructure

Infrastructure files for local development, Kubernetes, and cloud deployments.

## Layout

- `docker-compose.yml`: local PostgreSQL and application containers
- `k8s/`: local Kubernetes manifests
- `helm/`: reusable Helm chart
- `terraform/aws/`: AWS EKS and S3 infrastructure
- `terraform/gcp/`: GCP GKE and Cloud Storage infrastructure
